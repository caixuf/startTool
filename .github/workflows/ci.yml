name: KunAutoDrive CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Nightly (02:00 UTC) + manual trigger: run the long stability + benchmark
  # suites and the optional scenario-regression track (B2) that are excluded
  # from per-PR runs to keep pull-request CI fast.
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

# Least-privilege default for GITHUB_TOKEN across all jobs (read-only checkout).
permissions:
  contents: read

jobs:
  # ── scenario_file existence gate ──────────────────────────
  # Every scenario_file referenced in config/*.json must exist on
  # disk. Deleting a scenario file without updating its references
  # causes actors=0 + core dump at runtime (zhongkai_road_full.json
  # regression, 8eca3c7). This gate prevents that class of bug.
  # Symmetric with clock-service-gate.
  scenario-file-gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Reject missing scenario_file references in config/
        run: |
          set -e
          missing=0
          for f in config/*.json; do
            refs=$(grep -oP '"scenario_file"\s*:\s*"\K[^"]+' "$f" 2>/dev/null || true)
            for ref in $refs; do
              if [ ! -f "$ref" ]; then
                echo "::error::Missing scenario file: $ref (referenced in $f)"
                missing=1
              fi
            done
          done
          if [ "$missing" -eq 1 ]; then
            echo "::error::One or more scenario_file references point to missing files. Update the references or restore the file."
            exit 1
          fi
          echo "✓ All scenario_file references exist on disk"
      - name: Validate scenario and suite contracts
        run: python3 tools/scenarioctl.py validate

  # ── clock_service enforcement gate ─────────────────────────
  # clock_service.h must be the only time source for nodes. Raw
  # clock_gettime(CLOCK_MONOTONIC) in modules/ bypasses sim-time injection
  # (bag replay), breaking deterministic playback. This gate fails any PR that
  # reintroduces the raw call in a node plugin — nodes must use clock_now_us().
  # NOTE: src/core/clock_service.c itself is the legitimate single call site
  # (inside clock_now_us) and lives outside modules/, so it is not scanned.
  clock-service-gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Reject clock_gettime(CLOCK_MONOTONIC) in modules/
        run: |
          set -e
          if grep -rnE 'clock_gettime[[:space:]]*\([[:space:]]*CLOCK_MONOTONIC' modules/ ; then
            echo "::error::Forbidden: modules/ must use clock_now_us() from clock_service.h, not clock_gettime(CLOCK_MONOTONIC). See docs/book/06_clock_service.md."
            exit 1
          fi
          echo "✓ modules/ uses clock_now_us() exclusively"

  # ── topic contract gate ────────────────────────────────────
  # pipeline.json subscribe/publish must match NodePlugin s_inputs/s_outputs
  # (default profile: exact set equality; experimental/hw: JSON ⊆ code).
  # Prevents config/docs drift like safety_control omitting control/raw_cmd.
  topic-contract-gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Enforce NodePlugin topic contract vs config/pipeline*.json
        run: python3 ci/gates/topic_contract_check.py

  # ── zombie dashboard path ban ──────────────────────────────
  # The legacy Python HTTP dashboard was replaced by flowmond. Reintroducing
  # that retired entrypoint (or leaving orphaned .pyc) breaks the single
  # dashboard backend rule.
  zombie-ban-gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Reject retired Python dashboard entrypoint / orphan pyc
        run: python3 ci/gates/zombie_ban_check.py

  # ── map connectivity gate ──────────────────────────────────
  # 每个被 scenarios/*.json 引用的地图，main 链车道连通性回归检查
  # （tools/check_map_connectivity.py → astar_route.py --check）。
  # 地图数据（osm_to_map / grid / map_compiler）或 routes.json 改动后，
  # lane successors 一旦断开，demo 里 ego 立即开不动（osm_test 陆家嘴环路
  # 14.7m gap 前车之鉴）。与 scenario-file-gate 对称：纯数据检查、秒级完成、
  # 不依赖编译。已知断链在 check_map_connectivity.py 的 KNOWN_BROKEN 白名单
  # 中 WARN 不阻断，白名单外任何 FAIL 都 ERROR 阻断合并。
  map-connectivity-gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Reject broken main-chain lane connectivity in referenced maps
        run: python3 tools/check_map_connectivity.py

  # ── Standard build + test (Release) ────────────────────────
  build-release:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libcjson-dev python3 libsqlite3-dev
      - name: Configure (Release)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build build -j$(nproc)
      - name: Test (C++ Suites)
        run: ctest --test-dir build --output-on-failure --timeout 60 -LE "benchmark|manual|stability|integration"
      - name: Test (Python AI & Quant Suites)
        run: python3 -m unittest discover -s tests -p "test_*.py"

  # ── ASAN (Address Sanitizer) ───────────────────────────────
  build-asan:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: sudo apt-get update -qq && sudo apt-get install -y -qq libcjson-dev python3 libsqlite3-dev
      - name: Configure (ASAN)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DENABLE_ASAN=ON
      - name: Build
        run: cmake --build build -j$(nproc)
      - name: Test
        run: ctest --test-dir build --output-on-failure --timeout 120 -LE "benchmark|manual|stability|integration"
        env:
          ASAN_OPTIONS: detect_leaks=1:halt_on_error=0:log_path=/tmp/asan.log
      - name: Check ASAN log
        run: |
          if ls /tmp/asan.log.* 2>/dev/null; then
            echo "⚠ ASAN reports found (continuing — halt_on_error=0)"
            cat /tmp/asan.log.*
          else
            echo "✓ No ASAN issues"
          fi

  # ── TSAN (Thread Sanitizer) — disabled ────────────────────
  # 协程 + 无锁内存池的跨线程同步模式对 TSAN 产生大量假阳性
  # （frame_done_ 自旋、CAS freelist 等）。ASAN 已覆盖内存安全，
  # TSAN 待协程生命周期稳定后重新启用。
  # build-tsan:
  #   runs-on: ubuntu-24.04
  #   steps:
  #     - uses: actions/checkout@v4
  #     ...

  # ── Windows cross-compile gate (mingw-w64) ─────────────────
  # Cross-compiles the whole core project to native Windows x86_64 PE
  # binaries using mingw-w64 on a Linux runner — no Windows runner needed.
  # This is the reproducible regression gate for the Windows target: it
  # catches compat-layer / _WIN32-path breakage (header conflicts, missing
  # POSIX shims, atomic link deps) at PR time. MSVC-native builds still go
  # through CMakePresets (windows-vs / windows-ninja) on developer machines.
  build-windows-mingw:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Install mingw-w64 toolchain
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64 ninja-build python3
      - name: Configure (mingw-w64 cross)
        run: |
          cmake -S . -B build-mingw -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-w64-x86_64.cmake \
            -DCMAKE_BUILD_TYPE=Release
      - name: Build (mingw-gate targets only)
        # 只构建门禁校验的三个 Windows 二进制。全量构建会连带编译 kun_quant,
        # 其 sqlite3 依赖在 mingw 交叉环境不可用 (ubuntu 无 mingw 版 sqlite3 dev)。
        run: cmake --build build-mingw --target flow_launcher flowmond flowctl -j$(nproc)
      - name: Verify PE32+ Windows binaries were produced
        run: |
          set -e
          for exe in flow_launcher flowmond flowctl; do
            f="build-mingw/bin/${exe}.exe"
            [ -f "$f" ] || { echo "::error::missing $f"; exit 1; }
            file "$f" | grep -q "PE32+ executable" || { echo "::error::$f is not a Windows PE binary"; exit 1; }
            echo "✓ $f"
          done


  # Builds the separate adas_nodes .so plugin subproject and verifies that
  # flow_launcher loads every node (dlopen + ABI check), runs the pipeline
  # under a 15-second stress budget, and passes the ctest integration smoke.
  # Merged from former stress-test + integration-test to save one full build.
  integration-test:
    runs-on: ubuntu-24.04
    needs: build-release
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: sudo apt-get update -qq && sudo apt-get install -y -qq libcjson-dev python3 libeigen3-dev
      - name: Configure (Release)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - name: Build framework
        run: cmake --build build -j$(nproc)
      - name: Build node plugins
        run: |
          cmake -B build/modules/adas_nodes -S modules/adas_nodes -DFLOWENGINE_BUILD="$PWD/build"
          cmake --build build/modules/adas_nodes -j$(nproc)
      - name: Offline esmini chain check (adas_nodes ctest)
        # 预构建库格式门禁（configure 期）+ json_to_xodr → RM_Init → frenet↔world
        # 往返不变式。0ca4c09 曾把 Mach-O 覆盖 Linux .so 导致链接晦涩失败，
        # 此步在 CI 上把该类回归拦在集成冒烟之前。
        run: ctest --test-dir build/modules/adas_nodes --output-on-failure -R "esmini|road_network|pem_log_protocol"
      - name: Pipeline stress run (15s)
        run: timeout 35 ./build/bin/flow_launcher config/pipeline.json --duration 15
      - name: Integration smoke (ctest -L integration)
        run: ctest --test-dir build --output-on-failure -L integration

  # ── Algorithm evaluator (regression gate) ─────────────────────
  # Runs the full plugin pipeline (demo.sh) and scores it against
  # pass_criteria.  A FAIL blocks the PR — this is the quality gate
  # for any change touching the ADAS pipeline chain.
  evaluator:
    runs-on: ubuntu-24.04
    needs: build-release
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: sudo apt-get update -qq && sudo apt-get install -y -qq libcjson-dev python3 libeigen3-dev
      - name: Configure (Release)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - name: Build framework
        run: cmake --build build -j$(nproc)
      - name: Build node plugins
        run: |
          cmake -B build/modules/adas_nodes -S modules/adas_nodes -DFLOWENGINE_BUILD="$PWD/build"
          cmake --build build/modules/adas_nodes -j$(nproc)
      - name: Regression evaluator (45s demo)
        run: python3 ci/evaluators/demo_evaluator.py --duration 45 --interval 0.5
      - name: Pipeline integrity check
        run: python3 tools/pipeline_check.py --json /tmp/flow_topology.json

  # ── Visualization layer (flowmond) + algorithm regression ──
  # Builds flowmond (the C dashboard daemon; sole HTTP backend for FlowBoard)
  # and runs the pytest suite under ci/evaluators/ (control-node curve-fallback
  # regression etc.). The flowmond HTTP API is smoke-tested directly.
  viz:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libcjson-dev
          python -m pip install --upgrade pip -q
          pip install -q -r ci/evaluators/requirements-dev.txt
      - name: Configure (Release)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - name: Build flowmond
        run: cmake --build build --target flowmond -j$(nproc)
      - name: Algorithm regression tests (pytest)
        run: python -m pytest ci/evaluators -v
      - name: flowmond smoke test
        run: |
          ./build/bin/flowmond --port 8811 --html-path tools/flowboard/index.html &
          SRV=$!
          sleep 2
          curl -sf http://localhost:8811/api/health | tee /tmp/health.json
          echo
          curl -sf http://localhost:8811/api/topology > /tmp/topo.json
          kill $SRV 2>/dev/null || true

  # ── vis/ JS smoke tests (pure Node, no browser) ───────────
  # 2026-07 重建：新增 THREE shim + 全量模块加载 + tick 冒烟 + ESLint 门禁。
  # 与 C 侧 demo_evaluator.py 对称 —— 任何 tools/flowboard/** 改动
  # 必须通过此 job，否则不合并。
  vis-js-tests:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Syntax check all vis/ modules
        run: |
          set -e
          for f in $(find tools/flowboard/js/vis -name '*.js'); do
            node --check "$f"
          done
          for f in $(find tools/flowboard/js -maxdepth 1 -name '*.js'); do
            node --check "$f"
          done
      # ── 门禁 1: 全量模块加载（抓语法错/顶层 ReferenceError/import 路径）──
      - name: Module load smoke (26 vis/ modules)
        run: |
          set -e
          node --import ./tests/support/three-preload.mjs tests/vis_module_load.test.mjs
      # ── 门禁 2: ESLint no-undef (error) + no-unused-vars (warn) ──
      - name: ESLint no-undef + no-unused-vars
        run: |
          set -e
          npx --yes eslint -c tools/flowboard/eslint.config.mjs tools/flowboard/js/vis/
      # ── 门禁 3: 单帧 tick 冒烟（3 场景 × tickAnimation） ──
      - name: Render tick smoke (3 scenarios)
        run: |
          set -e
          node --import ./tests/support/three-preload.mjs tests/vis_render_tick.test.mjs
      # ── 全量 JS 门禁（与 npm run vis:check:all 一致，作者本地同命令） ──
      - name: Run vis smoke tests (vis:check:all)
        run: npm run vis:check:all
      # ── 3D 模型门禁：L/R 对称性 + 完整性（2026-07 转向灯左右标反在此拦截）──
      - name: Validate 3D models (gen_models --validate)
        run: python3 tools/flowboard/gen_models.py --validate

  # ── Nightly suite: debug + ubsan + coverage + stress + stability + benchmark ──
  # These jobs are excluded from per-PR runs to keep pull-request CI fast.
  # They run on the nightly schedule or when triggered manually.
  nightly:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: sudo apt-get update -qq && sudo apt-get install -y -qq libcjson-dev python3 lcov libeigen3-dev libsqlite3-dev
      # ── Debug build + test ──
      - name: Configure (Debug)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
      - name: Build (Debug)
        run: cmake --build build -j$(nproc)
      - name: Test (Debug)
        run: ctest --test-dir build --output-on-failure --timeout 60 -LE "benchmark|manual|stability|integration"
      # ── UBSAN build + test ──
      - name: Configure (UBSAN)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DENABLE_UBSAN=ON
      - name: Build (UBSAN)
        run: cmake --build build -j$(nproc)
      - name: Test (UBSAN)
        run: ctest --test-dir build --output-on-failure --timeout 120 -LE "benchmark|manual|stability|integration"
      # ── Coverage build + test + report ──
      - name: Configure (Coverage)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DENABLE_COVERAGE=ON
      - name: Build (Coverage)
        run: cmake --build build -j$(nproc)
      - name: Test (Coverage)
        run: ctest --test-dir build --output-on-failure --timeout 360 -LE "benchmark|manual|stability|integration"
      - name: Generate coverage
        run: |
          lcov --capture --directory build --output-file coverage.info --ignore-errors mismatch,empty,unused,negative
          lcov --remove coverage.info '/usr/*' '*/_deps/*' '*/build/*' --output-file coverage.info --ignore-errors unused,empty
          lcov --list coverage.info --ignore-errors unused
      # ── Stress test (15s pipeline) ──
      - name: Build node plugins
        run: |
          cmake -B build/modules/adas_nodes -S modules/adas_nodes -DFLOWENGINE_BUILD="$PWD/build"
          cmake --build build/modules/adas_nodes -j$(nproc)
      - name: Stress test (15s pipeline run)
        run: timeout 35 ./build/bin/flow_launcher config/pipeline.json --duration 15
      # ── Long-running tests (stability + benchmark) ──
      - name: Long-running tests (stability + benchmark)
        run: ctest --test-dir build --output-on-failure --timeout 900 -L "stability|benchmark"
      # ── Scenario regression vs baseline ──
      - name: Scenario regression vs baseline
        run: python3 ci/evaluators/scenario_regression.py --baseline
      # ── Driving school exam gate (plan phase 2) ──
      # 与 scenario_regression.py 并列的另一档门禁：tools/driving_test.py 跑
      # 驾校全科，--ci 门禁不及格(任一项目<90)、--baseline 门禁单项分数较
      # baseline 下降>5。本期仅科目二(subject2_sim.py)实跑，科目一/三/四标
      # NOT_IMPLEMENTED 不阻断。
      # continue-on-error: 过渡保护——subject2_sim.py 与 baseline 尚在并行
      # 落地中；两者合入后删除 continue-on-error 即转为硬门禁（与 scenario
      # regression 同档阻断合并）。
      - name: Driving school exam vs baseline
        continue-on-error: true
        run: python3 tools/driving_test.py --all --ci --baseline